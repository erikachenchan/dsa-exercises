package org.worksheet;

public class BinarySearch {
    //When the data searched is in order, searching can be implemented a lot more efficiently than in linear search.
    // The idea behind Binary Search is to start looking for the searched value in the middle index of the array (or list),
    // compare the value found there to the searched value, and if needed (i.e, when the value isn't found there)
    // eliminate half of the search area.

    public static void main(String[] args) {

    }
}
